name: Deploy to Hostinger

on:
  push:
    branches:
      - main  # ou a branch que você deseja usar para deploy

jobs:
  ftp-deploy:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Deploy to Hostinger via FTP
      uses: SamKirkland/FTP-Deploy-Action@4.3.0
      with:
        server: ${{ secrets.FTP_SERVER }}
        username: ${{ secrets.FTP_USERNAME }}
        password: ${{ secrets.FTP_PASSWORD }}
        protocol: 'ftp'
        local-dir: ./  # Diretório local a ser enviado (ajuste conforme necessário)
        server-dir: /home/u202633271/domains/limpleve.com.br/public_html  # Diretório de destino no servidor
